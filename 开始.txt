# 开始



1. 打开 **Cursor** → 文件 → 打开文件夹 → 选本仓库根目录（克隆后的 `juno-agent` 文件夹）

2. 改 **USER.md**、**SOUL.md**（填写你的称呼与偏好）

3. 读 **BOOTSTRAP.md** 做第一次试跑

4. 运行 `scripts\启动Juno.bat` → 独立聊天

5. 任何项目里都可 `@my-core-agent`（写代码时用 Cursor）

6. 运行 `scripts\启动训练台.bat` → 训练界面



总部路径：本仓库根目录（`./`）

